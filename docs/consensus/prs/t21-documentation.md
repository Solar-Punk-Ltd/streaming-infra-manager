# docs: reconcile remediation behavior and verification limits (T21)

Local branch `codex/t21-documentation` at `c640b10`, based on main-v2 `d046ebf`. Not pushed. The seven newer documentation commits and their bounded independent reviews are complete. Final synchronization with the remaining T01/T11/T04b/T09/T20 work remains pending.

The feature pages described old guarantees, including automatic balance-based transfer completion, guaranteed config recovery and version controls that differed from the agreed roadmap. They now describe immutable build identity, owned engine recovery, transaction evidence and authenticated test boundaries. Each page separates the live baseline from prepared local behavior and identifies incomplete acceptance work.

The integration README carries T10's authenticated, explicitly declared test target and its confirmed-resource inventory, atomic ownership checks and aggregate cleanup failures. It gives the correct Compose project/file and distinguishes the development API port from the web proxy. It records the four-rung ABR capacity and distinguishes deployment integration, disposable SQL, offline browser tests and engine-container harnesses. The version page now explains deletion markers and partial-cleanup refusal. Transaction examples include the implemented account/revision preconditions and separate completed browser behavior from pending physical-transport qualification. No STATE.md or CONTEXT.md was invented. Those optional references were investigated, and all25 remediation rows have local issue records.

Validation: source and accepted-scope comparison, Markdown paths checked and `git diff --check` passes. These are prose changes, so no application test suite was run for them. Fable's prior OME arm64 execution is reported as recorded evidence. This change makes no claim of an amd64, CI or live-host run.

The latest separate corrections are `1e6d436` for version removal, `eb0ca57` for transaction guards, `c5b1cfe` for cleanup, `37a3824` for independently validated identities from a partial creation response, `e8f2ae3` for configured settings, `acbb5f5` for the observation invalidation trigger and `c640b10` for captured-build settings admission. The T09 worker accepted the version, transaction and settings changes after source/evidence comparison. The T11 worker identified the cleanup wording correction, accepted its final diff and reviewed the final captured-build update. Earlier source and review history remains intact. The final draft must be refreshed after the remaining implementation corrections settle.
