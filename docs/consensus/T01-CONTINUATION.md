# T01 operation ownership continuation, 2026-09-09

## Active instance-scope correction,2026-09-09

**Accepted:** GREEN `b33aab817fd4eacad79acd58ceadaac65302cc5e`, parent RED `e988149`, is independently reviewed. It changes three source files through one shared instance predicate and resolves no holds. All18 new and25 existing affected SQL cases pass, plus manager types. Logs `/private/tmp/t01-operation-hold-scope-green.log`, `/private/tmp/t01-operation-hold-scope-existing-sql.log` and `/private/tmp/t01-operation-hold-scope-manager-types.log`. Both sessions69016/94326 ended. T04b received the validation slot for corrected types. Real caller admission RED is now the active step.

OperationHoldScope RED ran18 actual SQL cases with6 intended failures and12 passing controls in `/private/tmp/t01-operation-hold-scope-red.log`. Session38444 ended. Other/replaced-instance holds incorrectly block removal preflight, port handover and owned removal. Same-instance holds still block regardless of old intent. Fully and partially historical ownership stays conservative. Resolved holds do not block. The correction must leave every hold unchanged. Predicate GREEN and real caller admission wiring are next. Validation handoffs between T01 and T04b may happen directly by message without another root scheduling approval, preserving non-overlap and exact logs.

## Shared runtime completion contract,2026-09-09

Cross-provider review, OpenAI-hosted. Root, T01 and T04b agree on this integration seam. A prepared launch carries T01's final profile owner (instance, intent, config revision and version), exact BuildDescriptor/job reference, captured recovery artifact evidence, requested services and already-admitted DeployAttempt.id. T04b persists execution, attempt, job and launcher token before any possible spawn. It consumes that attempt without opening another attempt or taking another pre-job snapshot.

T04b exposes a durable immutable creator-completion receipt key and a transaction-local validator using the caller's PoolClient and exact execution/attempt/job/owner identities. The validator returns persisted provenance and never retires resources. A receipt proves only that the exact creator cannot create more work. A complete fresh daemon-bound observation after the receipt supplies service attribution separately. T01 commits current job container/snapshot observations and status/config lifecycle only behind exact ownership. Initial RUNNING/afterRunning starts any required engine watch. It does not release operation holds. Primary ancestry release waits for the owned applied/reverted terminal result, complete required-service coverage and the full healthy watch. An uncertain or interrupted watch retains all operation holds.

Shared lock order is allocation, daemon, sorted versions, profile, operations/references, then exact execution/attempt receipt rows. T04b audits every writer taking both when implementing030. No mismatch was found with the accepted read-only observer, which grants no cleanup permission. This contract permits caller wiring now. It is not evidence that runtime launch, completion or release is implemented.

Cross-provider review, OpenAI-hosted. This is the current implementation handoff for the remaining T01 integration. Earlier independent T01 work remains in Git and its PR draft. Repository search found no prior continuation or deletion at this path.

## Ownership and dependencies

**Latest accepted explicit restoration:** exact GREEN `5e0f785a2bb13474cbed1df71c4cec9786bebb64`, parent RED `2c75ef7`, is accepted after separate-worktree source and regression review. Migration029 records immutable restore ancestry at insertion. Explicit restore creates a fresh intent, operation, job and independent saved-build hold in one transaction while retaining source history and every old hold. Explicit23 plus affected automatic40 SQL cases pass63/63, no skips or cancellations, in `/private/tmp/t01-explicit-restore-sql-green.log`. Manager types pass in `/private/tmp/t01-explicit-restore-manager-types.log`. The41 focused cases are covered across the documented missing-environment setup rerun. SQL session17868 ended. No caller, runtime or hold-release acceptance is implied. Next, complete the already approved instance-scoped T06/T10 operation-hold predicates, then real service admission wiring. Exact-job ancestry release awaits T04b's durable creator completion and must not treat timestamp-based ledger observation as that authority.

**Current recovery checkpoint:** GREEN `c31e06da50998612148556bb6c0359d8f405d4a7` is accepted after exact source review and40/40 actual SQL cases in `/private/tmp/t01-retained-recovery-sql-green-actual.log`. RED `e62195b` had34 failures and six controls. Manager types pass in `/private/tmp/t01-retained-recovery-manager-types-green.log` after one result-union annotation. Original type and sandbox-EPERM setup logs are retained. Seven source files include only the shared snapshot annotations and saved-A admission boundary. No hold release or caller activation is part of this commit. The two prior inactive caller failures remain. Explicit-restore RED `2c75ef7b92e11720dbfa63907919c8d6638cef0c` is committed. All23 cases reach the intended missing API across the original22 and corrected launch-uncertain fixture rerun in `/private/tmp/t01-explicit-restore-sql-red-fixture.log`. These are missing-API specifications. Migration029 and the explicit transaction are implemented locally, awaiting manager types,23-case GREEN and40 affected automatic-recovery checks. The41 focused non-SQL checks passed across the original24 and unchanged17-case ownership rerun after supplying the missing synthetic DATABASE_URL setup.

**Remaining connected lifecycle approved:** after retained-A acceptance, finish owned hold release and known-unlaunched restoration, then wire the final atomic claims and exact-job successful observation/cleanup. Include one complete offline failed-rollout, watch, saved-A restoration and verified completion/release flow with restart and ambiguity controls. The two split-caller failures remain until the real service calls use the final transaction.

Explicit interrupted restore is a fresh operator intent, fresh job and NEW restore-previous operation. Migration029 is reserved for this kind and immutable source-operation linkage. Copy the source's verified saved-A descriptor/restoration target and create a new independent A hold under the new ownership. Never retag committed jobs or restore the failed candidate as a fallback. Preparation verifies A outside locks. Final allocation, daemon, sorted versions, profile and sorted operation/reference locks recheck exact instance, revisions, status, captured pointer, interrupted source and its unresolved hold. Explicit action may claim a captured NULL pointer, while automatic recovery still refuses it. Restoration, revisions, source supersession, lineage, new operation/hold/job, ports and guard commit together.

Retain operation holds through admission, cancellation, interruption and ambiguity. Exact-job success may release only proven primary operation holds through immutable ancestry. Test originalA interrupted, restoreB interrupted, restoreC succeeds, releasing exactly those primary holds while retaining independent job/execution/snapshot and unproven extra holds. Missing, cyclic or mismatched ancestry remains uncertain. Linkage is immutable from insertion, including no later NULL backfill inventing history. A separate explicit supersession lifecycle can end proven recovery authority. Additional observed holds need actual service coverage. Include concurrent requests, replacement during scan, A/B/C publication, every transaction failure, restart and exact release controls.

Retained-A refinement: automatic recovery keeps applying/watching/reverting only. Narrow its prepared request so no caller-selected version can replace the saved artifact. Share a `DeployVersionSnapshot` containing exactly id/name/rootPath/layout/buildId/commitSha/contract with BuildDescriptor consumers that need only those fields. Ordinary claims keep their full live-row admission. Never fill historical A's unrelated metadata from current B/C.

Automatic recovery requires the profile's active job pointer to match the operation's recorded current job, even if that exact old job hold has resolved through positive observation. A NULL pointer is not cancellation proof because a same-intent successor can claim and cancel back to NULL. Refuse and retain the hold, marking interrupted only through complete owner/status/pointer CAS. Explicit restore from an interrupted operation remains a distinct later API using saved A authority. It must not wrap A's previous config in an ordinary rollout on current B/C. Verify-now is a separate explicitly reviewed action.

Exact T01a dependency merge `1804f8994db37184ec2f91975882e01bb4199e57` is accepted, with exact parents `d20d559` and `115b194`. The inspect type retains current labels/ports/network metadata and reads Docker's top-level restart count. The real-watcher fixture keeps current terminal/hooks behavior, omitting the obsolete imported wrapper. Only the paused-container fixture needed the required inspect fields. All 57 focused cases and manager types pass in `/private/tmp/t01-inspect-dependency-{focused,manager-types}.log`. No persistence change or SQL repeat. Retained-A repository recovery is now the approved active slice, with release and service/success wiring still excluded.

Cross-provider review, OpenAI-hosted. Root accepts clean 028 GREEN `d20d55910c3b4a9e3adfefc713eab1663db82a6f` after exact source and regression review. Final relevant evidence is 21 hold SQL, 32 atomic-claim SQL, 36 unchanged build-snapshot SQL and 67 file cases, all passing. Manager types exited zero with the normal empty log `/private/tmp/t01-recovery-hold-manager-types-green.log`. The earlier full manager result remains 1141/1143 with two known caller failures. The broad SQL log remains incomplete. This is acceptance of descriptor/independent-hold production only.

Before retained-A recovery or service activation, merge exact accepted T01a `115b1944085ae759d3d757a52d132f482022693b` as a separate dependency commit after 028 acceptance. Root verified it is not an ancestor of `068bc85` or `58eb319`. Their ContainerControl still reads `State.RestartCount`, while T01a corrects the top-level inspect field. Run focused inspect/watch regressions after the merge. This is an existing agreed dependency, not a new task or a main-v2 merge.

The uncommitted 028 production slice has 21 recovery-hold SQL cases and 32 unchanged atomic-claim SQL cases passing in separate sequential runs. Logs `/private/tmp/t01-recovery-hold-sql-focused-final.log` and `/private/tmp/t01-recovery-atomic-claim-sql-final.log`. Full manager passed 1141/1143 with the same two inactive caller failures, and workspace types passed before the later helper correction. The broad SQL log was stopped by its owner and remains incomplete. Do not call that broad run green.

Pre-read name/version validation reproduced two real failures and now passes 56 affected file tests. Root found a second new-helper issue: `evidenceOf` bounded and hashed manifest bytes, then independently reopened the path through `readBuildManifest`. The worker is adding a deterministic replacement test and extracting the existing field rules into a pure parser, so the recorded hash and parsed identity come from the same bounded bytes. Existing `lockDeploySnapshot` still has its inherited manifest reader, so this correction alone does not establish a bounded-read guarantee for every admission path. Retained-A recovery, hold release and caller activation remain later slices.

The worker recorded three schemas left by the interrupted synthetic run in `/private/tmp/t01-recovery-interrupted-schemas.json`. In `t04b_test`: `t04b_execution_226e5f879667d011` and `t04b_revision_18feacd0eec6c4a5`. In `t01_test`: `t01_config_claim_e108cb311e972d38`. They are retained. Only the owning worker may clean them. No shared or live database was accessed.

Current 028 RED is `558df0c`. The new SQL matrix has 17 intended failures and one passing missing-source refusal control. It covers stored descriptor/hold production, historical nullable fields, atomic rollback if hold insertion fails, exact manifest/complete evidence and the outside-transaction capture seam. The file-helper RED is a missing module, not individually executed behavioral failures. Logs `/private/tmp/t01-recovery-hold-red.log` and `/private/tmp/t01-recovery-descriptor-red.log`. Representation and hold production implementation is active. Recovery and release remain untouched.

Latest dependency checkpoint `068bc85ed5d616669ca260696712cb6a7a52fc48` is clean and independently accepted. Its exact parents are `0f7d6ba` and T04a `c55c9d9`. The only production conflict retains both deletion-marker admission and the exact active-job ownership check. The imported removal fixtures preserve two isolated reference/FK lock-order cases plus two valid real-ledger cases. Assigned ownership and reference-only retention are tested separately. Partial-removal and execution fixtures now supply valid captured owners and active references, so the deletion marker remains the reason for refusal.

All 299 applicable SQL cases are covered. The combined run passed 298/299 and one existing five-second materializer interleaving timed out under load. The unchanged 18-case rerun passed. Manager 1127/1129 has only the two known engineConfigCreationGuard split-caller failures. Workspace types and 47 focused marker/path checks pass. Logs `/private/tmp/t01-removal-merge-{types-green,focus,unit-final,sql-green,materializer-rerun}.log`. The 028 representation and hold-production RED/GREEN slice is now authorized to start. Retained-A recovery, release and service activation still require their separate review checkpoints.

`implement_t08_completion` owns `/private/tmp/t01-codex` on `fix/t01-config-ownership`, starting from accepted `cff0f07`. The next authorized step is an explicit local merge of exact T10 `284790c96547a0a3d6f75e4d120d676257874fc0`, then exact T04b `58eb319787a64b3e2b80a7c53e1782f0d2689187`. T10 merge is `c52eb62` and T04b merge is `5bc3baa`. Root verified both exact parents and accepted the two mechanical finalizer conflict resolutions. Dependency validation is accepted. Manager tests pass 1091 of 1093, with the same two T01 failures. Common 289, frontend 18 and workspace types pass. Combined SQL initially passed 191 with 14 failures from missing `t10_test`. After creating that authorized synthetic database, all 14 exact cases passed. Thus 205 applicable SQL cases are covered. Logs `/private/tmp/t01-dependency-merge-{unit,common,frontend,types-green,sql,t10-sql}.log`.

T04b is left clean. Its core job claim, error fence and descriptor documentation are accepted. All 191 SQL checks and workspace types pass. Manager checks pass 961 of 963. The two engineConfigCreationGuard tests fail because operation begin changes the job reservation's captured config/intent revisions. Keep the strict error guard. Do not retag the committed job or treat this dependency as fully green.

The worker retains its own synthetic PostgreSQL container `285c10f9f8ae41c877fb54fe88f53e2ec3b8bb9ae3808872c02cee74bbadbe9a` on loopback 62527. It may add synthetic `t01_test`. Synthetic `t10_test` was also authorized and created for the accepted dependency checks. T04a owns the separate 61175 fixture. No worker cleans another's resources. Migration 028 is reserved to T01 operation lifecycle.

## Reviewed direction, with one proof still being refined

Use one final transaction to capture profile ownership, previous config, new config and revisions, operation, rollback target, build job, port plan and creation guard. Read previous config from the locked owned profile. Apply/reset advances config revision and intent once. Revert advances config revision while preserving the operation's intent. Final job insertion uses the resulting ownership. Failure rolls everything back. Returned reservations carry that exact profile and reference.

Read network evidence before the transaction. Existing bounded completeness, manifest and deletion-marker reads remain under the version lock because they close the proven artifact/prune race. Network calls, full-tree scans and payload/config writes are excluded there. Use allocation lock, then daemon attempt advisory lock, sorted version rows, profile, operation and references. Extract transaction-local primitives instead of nested pool transactions. The admitted attempt is consumed by runReserved, not replaced by a second attempt. An uncertain crash retains its job, operation and guard. Known-unlaunched cancellation must match exact identities.

The snapshot proof is now approved: under the daemon advisory lock, refuse any unresolved same-project attempt and capture the latest retained daemon/project attempt ID, then commit before reading Docker. Under the final transaction's same daemon lock, require no unresolved attempt and the unchanged latest ID. An intervening attempt changes retained history even after it resolves. The token is history generation, never job ownership. Tests cover a newly opened and resolved intervening attempt, and an already-open attempt being refused before any Docker snapshot. Current production code never deletes attempt history. A future purge must preserve this guarantee. No network work belongs under SQL locks.

Pin recovery to the rollout's code artifact and its stored previous config. Preserve separately observed pre-rollout artifact provenance as additional conservative holds. Never infer historical recovery authority from a same-name snapshot. Historical operations without pinned descriptors remain interrupted pending an explicit owned action.

Operation holds carry explicit instance and intent. Applying, watching, reverting and interrupted retain them through restart and failed observation. Owned terminal handoff can release only its exact operation holds after coverage is proved. Failed or superseded recovery holds can end when recovery is impossible, while independent job, execution and snapshot holds remain. T10 removal and T06 release predicates must become instance-scoped for new explicit operation holds. Ambiguous historical holds remain conservative.

## Accepted history primitive

RED `7975260` exposed six ignored-token admissions, two missing capture APIs and one passing unrelated-project control. GREEN `711fe3c` is independently accepted after 12 actual SQL cases, 22 unit cases and workspace types. It includes real advisory-lock waiting, caller rollback/retry and SERIAL-boundary text identity. Logs `/private/tmp/t01-attempt-history-{red,green,final-sql,unit,types,final-types}.log`. The optional token supports old callers only. New atomic admission requires it, and ordinary runJob wiring must capture it in a later separate regression. No caller or operation-lifecycle activation occurred at this checkpoint.

## Separate implementation checkpoints

The 028 direction is now approved in stages. First merge exact T04a `c55c9d93b17b75619b239104099de28cdfff2a43`, review actual conflicts and validation, then implement representation/hold production. Add nullable recovery descriptor, exact recovery-reference id and current deployment-job id without historical backfill. Decode an explicit immutable-build versus legacy-unproven union. Immutable evidence includes captured version/name/root identity, build/commit/contract, full artifact digest and exact manifest/complete hashes. Derive safe roots from the configured parent. Hash full trees outside SQL locks, then repeat bounded evidence and ownership checks under locks. No mutable source caller activation before the T04b copy/capture prerequisite is ready.

Retained-A recovery reads locator fields, then takes allocation, daemon, sorted involved version rows, profile, operation and reference locks. Re-read descriptor, exact unresolved hold and current job/instance/intent/config ownership. Current B/C must not replace A or prevent an otherwise proven retained-A recovery. Missing, changed, historical or legacy-unproven authority interrupts with no config/job/port/attempt mutation. Revert gets a new exact job but retains the original descriptor/operation hold. Additional observed pre-rollout artifacts are only conservative holds. A newly discovered unlocked source requires re-preparation.

Applying, watching, reverting and interrupted retain their holds. Cancelling a known-unlaunched job does not itself authorize releasing its operation hold. Release requires the same owned transaction to complete the previous-config restoration with a terminal outcome or otherwise end recovery authority. Applied/reverted release waits for the later exact-job successful observation handoff. Failed/superseded release must match actual loss of recovery authority. Never release independent job, execution or snapshot holds. Historical ambiguous holds remain conservative. T06/T10 operation-hold predicates become instance-scoped only for explicit ownership. Release alone must not acquire a version lock after a profile lock.

Separate RED/GREEN review checkpoints cover descriptor/hold production, retained-build recovery, then release/instance scope. Required SQL/file cases include A/B/C prune in both lock orders, same-commit rebuilds, malformed/tampered/missing evidence, descriptor mutation during verification, restart and failed observation, legacy/historical refusal, exact terminal coverage and unrelated owned holds. Caller and successful-completion integration still follow later.

Atomic repository seam `0f7d6ba203fc41c68c11690377355318ba2bbbda` is clean and independently accepted. RED `f4e8450` specifies 32 missing-API contract cases. GREEN passes all 32 and the complete 252 applicable SQL cases with no skips, plus workspace types. Full manager is 1091/1093, still the two old split-caller engineConfigCreationGuard failures. Logs `/private/tmp/t01-config-claim-{red,green-first,sql,unit,types}.log`. The new capture/begin/revert APIs are inactive. Config, operation, final job, ports and creation guard share one transaction. The earlier public ledger path composes the extracted locked-profile and final-reference helpers.

Next is the migration 028 design review. Specify pinned descriptor identity, operation-hold creation/release, sorted version locks, restart and A/B/C prune cases. Immutable recovery A must not substitute current B. A mutable legacy path alone is not evidence that A survived edits. Legacy recovery needs the T04b captured immutable execution/artifact evidence or an explicit unresolved state until that dependency is ready. This is an integration requirement, not a removed feature. Service/success wiring remains later. Root also recorded the remaining name-only startStop claim/terminal boundary for the subsequent ownership audit.

Transaction-local port planning is independently accepted at `2499de3`, after RED `abd24b4`. Three caller-owned transaction cases and 17 existing T06 SQL cases pass, plus manager types. Logs `/private/tmp/t01-port-transaction-{red,green,types}.log`. The helper leaves commit/rollback to its caller and preserves the allocation lock, conflict rules and held-service updates. Current public callers compose it without changing their boundary.

The next atomic begin/revert API remains inactive until lifecycle and caller review. Its final transaction requires the history token, exact target verification time and inventory seed identity, then rechecks ownership before writing config, operation, job, ports and attempt. Ordinary admission still requires the current version descriptor. Later rollback must authorize the operation's pinned artifact through its explicit retained hold, even if a newer artifact has been published. It must not weaken current-version admission or substitute the new artifact.

1. Exact dependency integration and reviewed transaction-local primitives.
2. Final atomic ownership and admission with actual SQL races. Close the two existing creation-guard failures without weakening their behavior.
3. Pinned operation descriptors and artifact-reference lifecycle, with migration 028 and pruning/restart tests.
4. Owned successful completion, snapshot transfer and cleanup. Integrate T10's terminal-event latch. A stale success must not upsert container data, resolve references, delete config files, mark RUNNING or call afterRunning. Evidence is collected outside SQL and committed only under exact instance, intent, config, version, status and active-job ownership. Bounded file cleanup needs the same ownership and exact protected-path checks.

Tests include competing saves, same-name replacement at equal config revision, apply/reset/revert final ownership, injected operation/job insertion failure, port/guard refusal before restoring config, both prune lock orders, publication A/B/C, interrupted restart, delayed success/failure against successors and both terminal events firing. Root reviews each checkpoint before the next broad integration.

No main-v2 merge, push, GitHub action, host access, real engine build, image pull, live funds or production activation is authorized. The explicit reviewer-only override for local work remains in force.
