# T21 documentation checkpoint, 2026-09-08

## Latest update, 2026-09-09

Latest documentation commit `c640b10` records the accepted T11 captured-build correction `ef269a8`. Its validation and admission share the selected build, with publication refused before state changes. Mutable inputs remain open. The T11 worker independently accepted the exact factual diff and its distinction between 28 focused checks, three SQL cases, manager types and the earlier full 1095/1097 run. No application tests were needed for this prose change. The T21 tree is clean at `c640b10`.

Root's additional engine-settings documentation commit `e8f2ae3` replaces the obsolete missing-literal limitation with the implemented configured-value/source behavior, stale observation handling, draft preservation and exact-job write guard. It records the accepted T11 test counts and leaves publication validation, mutable inputs and T01 caller integration open. The T09 worker independently accepted the exact diff against T11 `1684131` and its logs. Root committed its minor wording clarification as `acbb5f5`: observations disappear when the page receives a changed deployment/configuration revision, not through an out-of-band filesystem watcher. The task tree is clean at that commit.

Root committed three separate documentation corrections on the existing T21 branch. `1e6d436` documents guarded version removal and durable markers, while keeping T01 operation/success and T04b runtime-copy integration open. `eb0ca57` updates transaction API examples with account/revision preconditions and records completed browser work separately from pending transport qualification. `c5b1cfe` describes T10's confirmed-resource inventory, atomic cleanup and aggregate failures, without claiming a live integration run. Diff checks pass. These prose-only changes need no new code tests. The T09 worker will independently review the first two after its connector checkpoint. The T11 worker will review the cleanup document after its settings checkpoint. Final T11/T20 synchronization remains open. No main-v2 merge or push occurred.

Independent reviews are now received. T09 accepted the exact `1e6d436` and `eb0ca57` changes after source/schema/evidence comparison. T11 found one overbroad statement in `c5b1cfe`: a partially malformed response can still carry independently validated resource identities. Root confirmed the actual inventory code and committed the recommended wording in `37a3824`. T11 reviewed that exact correction and closed the bounded review. The corrected docs distinguish confirmed identities from unresolved coverage. The T21 tree is clean at that commit.

**Previous accepted checkpoint:** clean `b02503e`. Independent review used `/private/tmp/t21-review-codex` at `6c6edf8`. Corrections `347bf36` and `b02503e` fix the integration README's Compose project/file, distinguish local API9876 from proxy8080, record the four-rung ABR capacity and actual cleanup limitations, and update T04a/T06 test evidence. Diff checks pass. The local draft is `prs/t21-documentation.md`. Final synchronization with T11, T09, T10, T04b and T20 remains open. This is not full T21 acceptance.

Worktree `/private/tmp/t21-codex`, branch `codex/t21-documentation`, from main-v2 d046ebf. Local commits:69c8e76 stack versions,33bedfb engine validation/recovery,db0b7b4 authenticated test boundaries,6c6edf8 transaction states. Tracked clean after these commits. Diff checks pass. No tests were added or run for prose-only changes.

Root reconciled the docs against the accepted issues and branch source. The docs distinguish baseline/live behavior from local prepared changes, pending integration and owner inputs. Fable's recorded local arm64 OME evidence is identified as prior evidence, not a new run. No STATE.md or CONTEXT.md was manufactured. Both optional paths were investigated and absent. The issue directory has25 task rows plus its README.

The independent review of that bounded checkpoint is complete as recorded above. Final synchronization remains pending. T11's former literal-value gap is now fixed, with active-job/defaults integration still underway. T09's history and recovery UI are implemented, with physical transport qualification and runtime wiring still open. T10's confirmed-resource cleanup and identity guards are accepted locally. T04a now uses guarded version removal with durable markers. Reflect those final behaviors when their remaining dependencies settle. The local draft already exists at `prs/t21-documentation.md`. Do not mark the whole T21 row complete yet.
