manager/.env
# The credentials a session is handed for the running instance. Never committed.
/.env
# Written by deploy/deploy.sh so the server can name the bundled stack commit.
manager/.stack-commit
manager/node_modules
manager/dist
*.log
.DS_Store

node_modules
*.tsbuildinfo
manager/test/integration/env.itest

# Session scratch of the AI tools that work in this checkout, never part of the repository
.scratch/
